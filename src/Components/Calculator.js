import { useState } from "react";

function Calculator () {
    const [calculate , setCalculate]= useState(0);
    
    return (
        <div>
        <h1>Calculator</h1>
        </div>
    )
}

export default Calculator;